** start of script.js **

function findLongestWordLength(str) {
  let longeststr = '';
  let words = str.split(" ");
  for(let i=0;i<words.length;i++){
      let individualword = words[i];
      if(individualword.length>longeststr.length){
           longeststr = individualword;
      }
  }
  return longeststr.length;
}

findLongestWordLength('The quick brown fox jumped over the lazy dog');

// Passed:1. findLongestWordLength("The quick brown fox jumped over the lazy dog") should return a number.
// Passed:2. findLongestWordLength("The quick brown fox jumped over the lazy dog") should return 6.
// Passed:3. findLongestWordLength("May the force be with you") should return 5.
// Passed:4. findLongestWordLength("Google do a barrel roll") should return 6.
// Passed:5. findLongestWordLength("What is the average airspeed velocity of an unladen swallow") should return 8.
// Passed:6. findLongestWordLength("What if we try a super-long word such as otorhinolaryngology") should return 19.

** end of script.js **

