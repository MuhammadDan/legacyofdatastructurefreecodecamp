** start of script.js **

// Questions: Check if a string (first argument, str) ends with the given target string (second argument, target).

// This challenge can be solved with the .endsWith() method, which was introduced in ES2015. But for the purpose of this challenge, we would like you to use one of the JavaScript substring methods instead.


function confirmEnding(str, target) {
   let targetlength = target.length;
   let targettoMatch = str.slice(str.length-targetlength,str.length)
   if(targettoMatch===target){
     return true;
   }
   return false;
}

confirmEnding('Bastian', 'n');
// 1. confirmEnding("Bastian", "n") should return true.
// Passed:2. confirmEnding("Congratulation", "on") should return true.
// Passed:3. confirmEnding("Connor", "n") should return false.
// Passed:4. confirmEnding("Walking on water and developing software from a specification are easy if both are frozen", "specification") should return false.
// Passed:5. confirmEnding("He has to give me a new name", "name") should return true.
// Passed:6. confirmEnding("Open sesame", "same") should return true.
// Passed:7. confirmEnding("Open sesame", "sage") should return false.
// Passed:8. confirmEnding("Open sesame", "game") should return false.
// Passed:9. confirmEnding("If you want to save our world, you must hurry. We dont know how much longer we can withstand the nothing", "mountain") should return false.
// Passed:10. confirmEnding("Abstraction", "action") should return true.
// Passed:11. Your code should not use the built-in method .endsWith() to solve the challenge.

** end of script.js **

